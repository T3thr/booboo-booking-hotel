# 🚀 Quick Start - ระบบจองห้องพักแบบครบวงจร

## เริ่มต้นใช้งานภายใน 5 นาที!

### ขั้นตอนที่ 1: รัน Database Migration (1 นาที)

```bash
# Windows
cd database/migrations
run_migration_015.bat

# Linux/Mac
cd database/migrations
chmod +x run_migration_015.sh
./run_migration_015.sh
```

### ขั้นตอนที่ 2: Restart Backend (1 นาที)

```bash
cd backend
go run cmd/server/main.go
```

รอจนเห็นข้อความ:
```
Server is running on :8080
```

### ขั้นตอนที่ 3: Restart Frontend (1 นาที)

```bash
cd frontend
npm run dev
```

รอจนเห็นข้อความ:
```
✓ Ready in 12.2s
- Local: http://localhost:3000
```

### ขั้นตอนที่ 4: ทดสอบระบบ (2 นาที)

#### ทดสอบการค้นหาห้อง:
1. เปิด http://localhost:3000
2. คลิก "ค้นหาห้องพัก"
3. เลือกวันที่ (เช่น วันนี้ + 7 วัน ถึง วันนี้ + 10 วัน)
4. เลือกจำนวนผู้เข้าพัก (เช่น 2 คน)
5. คลิก "ค้นหา"
6. **ผลลัพธ์:** ควรเห็นห้องว่างพร้อมราคา ✅

#### ทดสอบการจอง:
1. คลิก "จองห้องนี้" ที่ห้องที่ต้องการ
2. กรอกข้อมูลผู้เข้าพัก
3. คลิก "ดำเนินการต่อ"
4. **ผลลัพธ์:** เข้าสู่หน้า Hold Booking พร้อม countdown timer ✅

#### ทดสอบการชำระเงิน:
1. คลิก "ดำเนินการชำระเงิน"
2. เลือกวิธีชำระเงิน (เช่น โอนเงินผ่านธนาคาร)
3. อัปโหลดรูปภาพใดๆ เป็นหลักฐาน
4. คลิก "ยืนยันการชำระเงิน"
5. **ผลลัพธ์:** เข้าสู่หน้า Confirmation ✅

#### ทดสอบ Admin:
1. Login ด้วย:
   - Email: `manager@hotel.com`
   - Password: `manager123`
2. เข้าเมนู "ตรวจสอบการชำระเงิน"
3. **ผลลัพธ์:** เห็นรายการหลักฐานที่เพิ่งอัปโหลด ✅
4. คลิกดูรายละเอียด
5. คลิก "อนุมัติ"
6. **ผลลัพธ์:** สถานะเปลี่ยนเป็น "อนุมัติแล้ว" ✅

---

## 🎯 ทดสอบเพิ่มเติม

### ทดสอบ Responsive Design:
1. เปิด DevTools (F12)
2. Toggle Device Toolbar (Ctrl+Shift+M)
3. ทดสอบบน Mobile, Tablet, Desktop
4. **ผลลัพธ์:** UI ปรับตามขนาดหน้าจออย่างสมบูรณ์ ✅

### ทดสอบ Hold Expiry:
1. เริ่มจองห้องใหม่
2. รอ countdown timer จนหมด (15 นาที)
3. **ผลลัพธ์:** ระบบ redirect กลับไปหน้าค้นหาอัตโนมัติ ✅

### ทดสอบ File Upload Validation:
1. พยายามอัปโหลดไฟล์ที่ไม่ใช่รูปภาพ (เช่น .pdf)
2. **ผลลัพธ์:** แสดง error "กรุณาอัปโหลดไฟล์รูปภาพเท่านั้น" ✅
3. พยายามอัปโหลดไฟล์ขนาดใหญ่ (> 5MB)
4. **ผลลัพธ์:** แสดง error "ไฟล์มีขนาดใหญ่เกิน 5MB" ✅

---

## 🐛 Troubleshooting

### ปัญหา: ค้นหาห้องแล้วไม่เจออะไร
**แก้ไข:**
```bash
# ตรวจสอบว่ามี seed data
cd database/migrations
psql -U postgres -d hotel_booking -f 013_seed_demo_data.sql
```

### ปัญหา: Backend ไม่ทำงาน
**แก้ไข:**
```bash
# ตรวจสอบ PostgreSQL
psql -U postgres -d hotel_booking -c "SELECT COUNT(*) FROM room_types;"

# ตรวจสอบ .env
cd backend
cat .env
```

### ปัญหา: Frontend ไม่ทำงาน
**แก้ไข:**
```bash
# ลบ node_modules และติดตั้งใหม่
cd frontend
rm -rf node_modules
npm install
npm run dev
```

### ปัญหา: อัปโหลดรูปไม่ได้
**แก้ไข:**
```bash
# สร้าง uploads folder
cd frontend/public
mkdir -p uploads/payment-proofs
chmod 755 uploads/payment-proofs
```

---

## 📊 ตรวจสอบสถานะระบบ

### ตรวจสอบ Database:
```sql
-- เช็คจำนวนห้อง
SELECT COUNT(*) FROM rooms;

-- เช็คจำนวน inventory
SELECT COUNT(*) FROM room_inventory;

-- เช็คจำนวนการจอง
SELECT COUNT(*) FROM bookings;

-- เช็คจำนวนหลักฐานการชำระเงิน
SELECT COUNT(*) FROM payment_proofs;
```

### ตรวจสอบ API:
```bash
# Test room search
curl "http://localhost:8080/api/rooms/search?checkIn=2025-01-15&checkOut=2025-01-18&guests=2"

# Test room types
curl "http://localhost:8080/api/rooms/types"
```

---

## ✅ Success Criteria

ระบบทำงานสมบูรณ์เมื่อ:
- [x] ค้นหาห้องและเห็นห้องว่างพร้อมราคา
- [x] Hold booking ทำงานพร้อม countdown timer
- [x] อัปโหลดหลักฐานการโอนเงินได้
- [x] Admin เห็นและอนุมัติ/ปฏิเสธได้
- [x] Responsive บนทุกอุปกรณ์
- [x] Error handling ทำงานถูกต้อง

---

## 🎉 เสร็จสิ้น!

ระบบจองห้องพักของคุณพร้อมใช้งานแล้ว!

**Next Steps:**
1. ปรับแต่ง UI/UX ตามความต้องการ
2. เพิ่ม email notifications
3. เชื่อมต่อ real payment gateway
4. Deploy ขึ้น production

**Happy Coding! 🚀**
