# ขั้นตอนการ Rebuild และทดสอบ

## ⚠️ สำคัญมาก!

คุณต้อง **REBUILD BACKEND** เพื่อให้โค้ดใหม่ทำงาน!

## ขั้นตอน:

### 1. Stop Backend ที่กำลังรันอยู่
```bash
# กด Ctrl+C ใน terminal ที่รัน backend
```

### 2. Rebuild Backend
```bash
cd backend
go build -o hotel-booking-api.exe ./cmd/server
```

### 3. Run Backend ใหม่
```bash
./hotel-booking-api.exe
# หรือ
go run cmd/server/main.go
```

### 4. ทดสอบ
```bash
# 1. ไปที่ /admin/reception
# 2. Approve booking 1 รายการ
# 3. ไปที่ /admin/checkin
# 4. ตรวจสอบว่าแสดง "💰 ชำระเงินแล้ว"
```

## สิ่งที่แก้ไขแล้ว:

✅ Backend ใช้ UPSERT แทน UPDATE
✅ สร้าง payment_proof record ถ้าไม่มี
✅ Payment status จะแสดงถูกต้อง

## ถ้ายังไม่แสดง "ชำระเงินแล้ว":

1. ตรวจสอบว่า rebuild backend แล้วหรือยัง
2. ตรวจสอบ backend logs
3. ตรวจสอบ database:
```sql
SELECT booking_id, status FROM payment_proofs WHERE booking_id = [your_booking_id];
```

## ถัดไป:

เพิ่มปุ่ม "ดูหลักฐานการชำระเงิน" ใน check-in page
