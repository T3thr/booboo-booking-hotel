# ===========================================
# PRODUCTION ENVIRONMENT - GO BACKEND
# ===========================================
# ⚠️  สำคัญ: แทนที่ค่า placeholder ทั้งหมดก่อน deploy!
# Configuration นี้เหมาะสำหรับ Vercel + Neon PostgreSQL

# ===========================================
# SERVER CONFIGURATION
# ===========================================
PORT=8080
GIN_MODE=release
ENVIRONMENT=production

# ===========================================
# DATABASE CONFIGURATION (NEON POSTGRESQL)
# ===========================================
# ดูได้จาก: https://console.neon.tech
# ใช้ "Pooled connection" string สำหรับ production
DB_HOST=ep-xxx-xxx.region.aws.neon.tech
DB_PORT=5432
DB_USER=your_neon_username
DB_PASSWORD=your_neon_password
DB_NAME=neondb
DB_SSLMODE=require

# Full Database URL (Neon Pooled Connection - แนะนำสำหรับ Vercel)
DATABASE_URL=postgresql://your_neon_username:your_neon_password@ep-xxx-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require

# Database Pool Configuration (Production Optimized)
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=10
DB_CONN_MAX_LIFETIME=10m

# ===========================================
# JWT & AUTHENTICATION
# ===========================================
# สร้างด้วย: openssl rand -base64 32
# ต้องมีอย่างน้อย 32 ตัวอักษรสำหรับ production!
JWT_SECRET=IfXTxsvIgT9p0afnI/8cu5FJSVAU8l5h9TDsupeUbjU=
JWT_EXPIRATION=24h

# ===========================================
# CORS CONFIGURATION
# ===========================================
# แทนที่ด้วย domain จริงของ frontend
FRONTEND_URL=https://your-frontend-domain.vercel.app
ALLOWED_ORIGINS=https://your-frontend-domain.vercel.app,https://www.your-domain.com

# ===========================================
# REDIS CACHE (OPTIONAL - แนะนำ UPSTASH)
# ===========================================
# สำหรับ production ใช้ Upstash Redis: https://upstash.com
# Format: redis://default:[password]@[host]:[port]
REDIS_URL=
# Example: redis://default:your_password@us1-xxx.upstash.io:6379

# ===========================================
# RATE LIMITING (PRODUCTION)
# ===========================================
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=1000
RATE_LIMIT_WINDOW=1m

# ===========================================
# LOGGING (PRODUCTION)
# ===========================================
LOG_LEVEL=warn
LOG_FORMAT=json

# ===========================================
# NIGHT AUDIT JOB
# ===========================================
NIGHT_AUDIT_ENABLED=true
NIGHT_AUDIT_TIME=00:00

# ===========================================
# HOLD CLEANUP JOB
# ===========================================
HOLD_CLEANUP_ENABLED=true
HOLD_CLEANUP_INTERVAL=5m
HOLD_TIMEOUT=15m

# ===========================================
# MONITORING & HEALTH CHECK
# ===========================================
ENABLE_METRICS=true
HEALTH_CHECK_PATH=/health

# ===========================================
# EMAIL CONFIGURATION (OPTIONAL)
# ===========================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@yourdomain.com
SMTP_TLS=true

# ===========================================
# ERROR TRACKING (OPTIONAL - SENTRY)
# ===========================================
SENTRY_DSN=
SENTRY_ENVIRONMENT=production

# ===========================================
# SECURITY (PRODUCTION)
# ===========================================
BCRYPT_COST=12
SESSION_TIMEOUT=3600
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_DURATION=900
