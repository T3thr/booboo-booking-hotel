# ============================================================================
# PRODUCTION ENVIRONMENT - Neon PostgreSQL (Vercel Deployment)
# ============================================================================
# ตั้งค่าใน Vercel Dashboard -> Settings -> Environment Variables
# Set these in Vercel Dashboard -> Settings -> Environment Variables
# ============================================================================

# Neon Database URL (Production - Pooled Connection)
# รูปแบบ: postgresql://[user]:[password]@[endpoint]/[database]?sslmode=require
# Get from: https://console.neon.tech -> Production Branch -> Connection Details
DATABASE_URL="postgresql://username:password@ep-xxx-xxx.region.aws.neon.tech/neondb?sslmode=require"

# Neon Direct Connection (สำหรับ Migrations)
# Direct connection for running migrations in production
DIRECT_URL="postgresql://username:password@ep-xxx-xxx.region.aws.neon.tech/neondb?sslmode=require"

# Next.js Configuration
NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"
NODE_ENV="production"

# NextAuth Configuration (ถ้าใช้)
NEXTAUTH_URL="https://your-app.vercel.app"
NEXTAUTH_SECRET="your-production-secret-key-here-generate-with-openssl-rand-base64-32"

# Telemetry
NEXT_TELEMETRY_DISABLED=1

# Analytics (optional)
NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_GTM_ID=

# Feature Flags
NEXT_PUBLIC_ENABLE_ANALYTICS=true
NEXT_PUBLIC_ENABLE_CHAT_SUPPORT=true

# API Configuration
NEXT_PUBLIC_API_TIMEOUT=30000
NEXT_PUBLIC_API_BASE_URL="https://your-app.vercel.app/api"

# Logging
NEXT_PUBLIC_LOG_LEVEL=error

# ============================================================================
# วิธีการตั้งค่าบน Vercel:
# ============================================================================
# 1. ไปที่ Vercel Dashboard -> Your Project -> Settings -> Environment Variables
# 2. เพิ่ม Environment Variables ทั้งหมดข้างต้น
# 3. เลือก Environment: Production
# 4. ตั้งค่า DATABASE_URL และ DIRECT_URL จาก Neon Console
# 5. Deploy โปรเจกต์: git push origin main
# 6. Vercel จะ auto-deploy และใช้ environment variables ที่ตั้งไว้
# ============================================================================

# ============================================================================
# Neon Branching Strategy (แนะนำ):
# ============================================================================
# - Main Branch (Production): ใช้สำหรับ Production
# - Dev Branch: ใช้สำหรับ Development/Staging
# - สร้าง Branch ใหม่ได้ที่ Neon Console -> Branches
# - แต่ละ Branch จะมี Connection String แยกกัน
# ============================================================================
